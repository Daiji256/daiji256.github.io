---
import { adjustText } from '../utils/adjustText';

interface Props {
  text: string;
}

const { text } = Astro.props;
const adjustTextHtml = adjustText(text);
---

<span set:html={adjustTextHtml} />
